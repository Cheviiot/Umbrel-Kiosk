# 🖥️ Umbrel Kiosk

[![Release](https://img.shields.io/github/v/release/Cheviiot/Umbrel-Kiosk?style=flat-square&cacheSeconds=60)](https://github.com/Cheviiot/Umbrel-Kiosk/releases/latest)
[![License](https://img.shields.io/github/license/Cheviiot/Umbrel-Kiosk?style=flat-square&cacheSeconds=60)](https://github.com/Cheviiot/Umbrel-Kiosk/blob/main/LICENSE)

**Fullscreen kiosk browser for UmbrelOS dashboard**  
Автологин, автовосстановление, киоск-режим.

---

## ⚡ Быстрый старт

```bash
# Скачать и запустить установщик
curl -fsSL https://raw.githubusercontent.com/Cheviiot/Umbrel-Kiosk/main/scripts/umbrel-kiosk.sh -o /tmp/kiosk.sh && sudo bash /tmp/kiosk.sh
```

Откроется интерактивное меню:

```
╔═══════════════════════════════════════════════════════════╗
║              🖥️  Umbrel Kiosk v1.1.0                      ║
╚═══════════════════════════════════════════════════════════╝

  1) 📥 Установить
  2) ⚙️  Изменить URL  
  3) 🔍 Проверить статус
  4) 🗑️  Полностью удалить
  5) 🔁 Перезагрузить
  0) ❌ Выход
```

---

## ✨ Возможности

- 🎯 **Полноэкранный режим** - Заблокированы Alt+Tab, F11 и т.д.
- 🔄 **Автовосстановление** - Перезапуск при сбое сети
- 🎛️ **Панель навигации** - Назад/Вперёд/Домой (Ctrl+Alt+N)
- ⚙️ **Сервисное меню** - Смена URL, перезапуск (Ctrl+Alt+U)
- 📦 **DEB пакет** - Нативная установка для UmbrelOS
- 🖱️ **BreezeX курсоры** - Программные курсоры (никогда не пропадают)
- 🖥️ **Wayland native** - Без X11, использует Cage compositor
- 🎮 **GPU оптимизации** - Intel/AMD/NVIDIA драйверы автоматически
- 🤖 **Автостарт** - Загружается при включении

---

## 📦 Установка

### Автоматическая (рекомендуется)

```bash
curl -fsSL https://raw.githubusercontent.com/Cheviiot/Umbrel-Kiosk/main/scripts/umbrel-kiosk.sh | sudo bash
```

### Ручная

```bash
# Скачать DEB пакет
wget https://github.com/Cheviiot/Umbrel-Kiosk/releases/latest/download/umbrel-kiosk_v1.1.0_amd64.deb

# Установить
sudo dpkg -i umbrel-kiosk_v1.1.0_amd64.deb
sudo apt-get install -f
```

---

## ⌨️ Горячие клавиши

| Клавиши | Действие |
|---------|----------|
| `Ctrl+Alt+N` | Панель навигации |
| `Ctrl+Alt+U` | Сервисное меню |
| `Ctrl+Alt+F2` | Переключиться на TTY2 |

---

## 🔧 Настройка

### Изменить URL

1. Запустите установщик
2. Выберите пункт 2 "Изменить URL"
3. Введите новый адрес
4. Перезагрузите систему

### Параметры командной строки

```bash
./umbrel-kiosk.AppImage [options]

--url=<url>      URL (по умолчанию: http://umbrel.local)
--insecure       Разрешить самоподписанные сертификаты
--dev            Включить DevTools (F12)
--no-kiosk       Оконный режим
```

---

## 🇷🇺 Русификация UmbrelOS

Скрипт для полноценного добавления русского языка в веб-интерфейс UmbrelOS.

**Что делает скрипт:**
- ✅ Устанавливает файл перевода `ru.json`
- ✅ Патчит `language.ts` для добавления русского в список языков
- ✅ Патчит `date-time.ts` для локализации дат
- ✅ Создаёт резервные копии изменённых файлов

### Установка русского языка

```bash
# Скачать и запустить
curl -fsSL https://raw.githubusercontent.com/Cheviiot/Umbrel-Kiosk/main/scripts/umbrel-russify.sh -o /tmp/russify.sh && sudo bash /tmp/russify.sh
```

После установки:
1. Откройте веб-интерфейс UmbrelOS
2. Перейдите в **Settings → Language**
3. Выберите **"Русский"** в списке языков

### Удаление русского языка

```bash
sudo bash /tmp/russify.sh --uninstall
```

### Проверка статуса

```bash
sudo bash /tmp/russify.sh --check
```

## 🐛 Решение проблем

### Киоск не запускается

```bash
# Логи
journalctl -u getty@tty1 -f

# Проверить автологин
cat /etc/systemd/system/getty@tty1.service.d/autologin.conf

# Тест X11
sudo su - kiosk
startx
```

### Выход из киоска

- `Ctrl+Alt+F2` - переключение на TTY2
- SSH: `sudo pkill -f electron`

---

## 🗑️ Удаление

Запустите установщик и выберите пункт 4:

```bash
curl -fsSL https://raw.githubusercontent.com/Cheviiot/Umbrel-Kiosk/main/scripts/umbrel-kiosk.sh | sudo bash
```

Или удалите вручную:

```bash
sudo dpkg -r umbrel-kiosk
sudo rm -rf /opt/umbrel-kiosk
```

Удаляется:
- `/opt/umbrel-kiosk`
- Конфигурация автологина
- Настройки X11
- Опционально: пользователь и пакеты

---

## 🔨 Разработка

```bash
git clone https://github.com/Cheviiot/Umbrel-Kiosk.git
cd Umbrel-Kiosk
npm install
npm start -- --dev --url=http://umbrel.local
npm run build  # Создаёт DEB пакет
```

---

## 📋 Требования

- **OS**: UmbrelOS / Debian / Ubuntu Linux
- **Arch**: x86_64 (amd64)
- **RAM**: 2GB+
- **Packages**: X11, systemd (устанавливаются автоматически)

---

## 📄 License

MIT © 2024
